{
  "name": "github-viwer",
  "version": "1.0.0",
  "description": "A github viewer application that display github accounts for an according given name",
  "default": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir ./dist"
  },
  "author": "Muhammed A. Farghali",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "core-js": "^3.36.1",
    "regenerator-runtime": "^0.14.1"
  }
}
